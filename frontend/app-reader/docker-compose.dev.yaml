version: “3.7”
name: dnd 
services:
  app-reader:
    build: .
    container_name: app-reader
    ports:
      - "8888:8888"
    volumes:
      - .:/usr/src/app
    command: /bin/bash -c "npm i && npm start"
    networks:
      - dnd_network
networks:
  dnd_network:
    external: true
