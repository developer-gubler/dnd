version: “3.7”
name: dnd 
services:
  dnd_api-creature-reader:
    container_name: dnd_api-creature-reader
    build:
      context: .
      dockerfile: Dockerfile
    image: dnd-api-creature-reader:latest
    ports:
      - 8180:8080
    networks:
      - dnd_network
networks:
  dnd_network:
    external: true
